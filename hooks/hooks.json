{
  "description": "Briefs Claude with the project's recent session history at the start of every session.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          { "type": "command", "command": "npx -y claude-hindsight@latest --plain" }
        ]
      }
    ]
  }
}
